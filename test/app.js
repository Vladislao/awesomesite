"use strict";
var request = require('supertest');
var should = require('should');
var app = require('../app');

describe('/', function(){
    it('init', function(){
        (true).should.be.true();
    });
});